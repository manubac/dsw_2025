# Wishlist (Favoritos) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir a usuarios (role: `user`) guardar CartaClass en favoritos y comparar precios entre vendedores desde una página dedicada.

**Architecture:** Nueva entidad `Wishlist` (userId + cartaClass nullable FK) con 3 endpoints REST. Frontend: botón corazón en CardDetail, página /wishlist con grid de favoritos + tabla de comparación por carta, opción en menú de Header.

**Tech Stack:** MikroORM 6, Express 5, TypeScript, React 19, Tailwind CSS

---

## File Map

| Action | File |
|--------|------|
| Create | `backend/src/wishlist/wishlist.entity.ts` |
| Create | `backend/src/wishlist/wishlist.routes.ts` |
| Modify | `backend/src/app.ts` |
| Modify | `vite-project/vite-project-ts/src/pages/CardDetail.tsx` |
| Create | `vite-project/vite-project-ts/src/pages/WishlistPage.tsx` |
| Modify | `vite-project/vite-project-ts/src/components/Header.tsx` |
| Modify | `vite-project/vite-project-ts/src/App.tsx` |

---

### Task 1: Entidad Wishlist

**Files:**
- Create: `backend/src/wishlist/wishlist.entity.ts`

- [ ] **Step 1: Crear la entidad**

```typescript
import { Entity, Property, ManyToOne, Unique } from "@mikro-orm/core";
import { BaseEntity } from "../shared/db/baseEntity.js";
import { CartaClass } from "../carta/cartaClass.entity.js";

@Entity()
@Unique({ properties: ['userId', 'cartaClass'] })
export class Wishlist extends BaseEntity {
  @Property({ type: 'number' })
  userId!: number;

  @ManyToOne(() => CartaClass, { nullable: true })
  cartaClass?: CartaClass;
}
```

- [ ] **Step 2: Verificar compilación TypeScript**

```bash
cd backend && npx tsc --noEmit 2>&1
```
Expected: sin errores.

---

### Task 2: Rutas Wishlist (backend)

**Files:**
- Create: `backend/src/wishlist/wishlist.routes.ts`

- [ ] **Step 1: Crear el archivo de rutas**

```typescript
import { Router, Response } from "express";
import { orm } from "../shared/db/orm.js";
import { Wishlist } from "./wishlist.entity.js";
import { CartaClass } from "../carta/cartaClass.entity.js";
import { Carta } from "../carta/carta.entity.js";
import { Valoracion } from "../valoracion/valoracion.entity.js";
import { authenticate, AuthRequest } from "../shared/middleware/auth.js";

export const wishlistRouter = Router();

// GET /api/wishlist — favoritos del usuario autenticado
wishlistRouter.get("/", authenticate, async (req: AuthRequest, res: Response) => {
  try {
    const em = orm.em;
    const actor = req.actor as any;
    const userId = actor.id;

    const entries = await em.find(
      Wishlist,
      { userId },
      { populate: ["cartaClass"] }
    );

    const result = await Promise.all(
      entries.map(async (entry) => {
        if (!entry.cartaClass) {
          return { id: entry.id, disponible: false, cartaClass: null, cartas: [] };
        }

        const cartas = await em.find(
          Carta,
          { cartaClass: entry.cartaClass },
          { populate: ["items", "uploader"] }
        );

        const cartasFormateadas = await Promise.all(
          cartas.map(async (carta) => {
            const stock = carta.items.getItems().reduce((sum, item) => sum + item.stock, 0);
            let rating = 0;
            let reviewsCount = 0;
            if ((carta as any).uploader) {
              const valoraciones = await em.find(Valoracion, {
                tipoObjeto: "vendedor",
                objetoId: (carta as any).uploader.id,
              });
              reviewsCount = valoraciones.length;
              rating = reviewsCount > 0
                ? valoraciones.reduce((acc, v) => acc + v.puntuacion, 0) / reviewsCount
                : 0;
            }
            return {
              id: carta.id,
              name: carta.name,
              price: carta.price ? parseFloat(carta.price.replace(/[^0-9.]/g, "")) : 0,
              rarity: carta.rarity,
              setName: carta.setName,
              image: carta.image,
              stock,
              uploader: (carta as any).uploader
                ? {
                    id: (carta as any).uploader.id,
                    nombre: (carta as any).uploader.nombre,
                    rating,
                    reviewsCount,
                  }
                : null,
            };
          })
        );

        return {
          id: entry.id,
          disponible: true,
          cartaClass: {
            id: entry.cartaClass.id,
            name: entry.cartaClass.name,
            description: entry.cartaClass.description,
          },
          cartas: cartasFormateadas.sort((a, b) => a.price - b.price),
        };
      })
    );

    res.json({ data: result });
  } catch (error: any) {
    res.status(500).json({ message: "Error al obtener favoritos", error: error.message });
  }
});

// POST /api/wishlist — agregar a favoritos
wishlistRouter.post("/", authenticate, async (req: AuthRequest, res: Response) => {
  try {
    const em = orm.em;
    const actor = req.actor as any;
    const userId = actor.id;
    const { cartaClassId } = req.body;

    if (!cartaClassId) {
      return res.status(400).json({ message: "cartaClassId requerido" });
    }

    const existing = await em.findOne(Wishlist, { userId, cartaClass: { id: cartaClassId } });
    if (existing) {
      return res.status(200).json({ data: existing, message: "Ya está en favoritos" });
    }

    const cartaClass = await em.findOne(CartaClass, { id: Number(cartaClassId) });
    if (!cartaClass) {
      return res.status(404).json({ message: "CartaClass no encontrada" });
    }

    const entry = em.create(Wishlist, { userId, cartaClass });
    await em.flush();

    res.status(201).json({ data: entry });
  } catch (error: any) {
    res.status(500).json({ message: "Error al agregar a favoritos", error: error.message });
  }
});

// DELETE /api/wishlist/:cartaClassId — eliminar de favoritos
wishlistRouter.delete("/:cartaClassId", authenticate, async (req: AuthRequest, res: Response) => {
  try {
    const em = orm.em;
    const actor = req.actor as any;
    const userId = actor.id;
    const cartaClassId = Number(req.params.cartaClassId);

    const entry = await em.findOne(Wishlist, { userId, cartaClass: { id: cartaClassId } });
    if (!entry) {
      return res.status(404).json({ message: "Favorito no encontrado" });
    }

    await em.removeAndFlush(entry);
    res.json({ message: "Eliminado de favoritos" });
  } catch (error: any) {
    res.status(500).json({ message: "Error al eliminar favorito", error: error.message });
  }
});
```

- [ ] **Step 2: Verificar compilación**

```bash
cd backend && npx tsc --noEmit 2>&1
```
Expected: sin errores.

---

### Task 3: Registrar wishlistRouter en app.ts

**Files:**
- Modify: `backend/src/app.ts`

- [ ] **Step 1: Agregar import y uso del router**

Agregar después del import de `mensajeRouter`:
```typescript
import { wishlistRouter } from "./wishlist/wishlist.routes.js";
```

Agregar después de `app.use("/api/mensajes", mensajeRouter);`:
```typescript
app.use("/api/wishlist", wishlistRouter);
```

- [ ] **Step 2: Verificar compilación**

```bash
cd backend && npx tsc --noEmit 2>&1
```
Expected: sin errores.

---

### Task 4: Página WishlistPage.tsx

**Files:**
- Create: `vite-project/vite-project-ts/src/pages/WishlistPage.tsx`

- [ ] **Step 1: Crear la página completa**

```tsx
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useUser } from '../context/user'
import { fetchApi } from '../services/api'

interface CartaVendedor {
  id: number
  name: string
  price: number
  rarity: string | null
  setName: string | null
  image: string | null
  stock: number
  uploader: { id: number; nombre: string; rating: number; reviewsCount: number } | null
}

interface FavoritoEntry {
  id: number
  disponible: boolean
  cartaClass: { id: number; name: string; description: string } | null
  cartas: CartaVendedor[]
}

export default function WishlistPage() {
  const { user } = useUser()
  const navigate = useNavigate()
  const [favoritos, setFavoritos] = useState<FavoritoEntry[]>([])
  const [loading, setLoading] = useState(true)
  const [expandido, setExpandido] = useState<number | null>(null)

  useEffect(() => {
    fetchApi('/api/wishlist')
      .then(r => r.json())
      .then(json => setFavoritos(json.data || []))
      .catch(() => {})
      .finally(() => setLoading(false))
  }, [])

  const handleRemove = async (cartaClassId: number) => {
    await fetchApi(`/api/wishlist/${cartaClassId}`, { method: 'DELETE' })
    setFavoritos(prev => prev.filter(f => f.cartaClass?.id !== cartaClassId))
  }

  const getBadge = (entry: FavoritoEntry) => {
    if (!entry.disponible || !entry.cartaClass) {
      return <span className="px-2 py-0.5 rounded-full text-xs bg-gray-200 text-gray-500">No disponible</span>
    }
    if (entry.cartas.length === 0) {
      return <span className="px-2 py-0.5 rounded-full text-xs bg-gray-200 text-gray-500">Sin publicaciones</span>
    }
    const conStock = entry.cartas.filter(c => c.stock > 0)
    if (conStock.length === 0) {
      return <span className="px-2 py-0.5 rounded-full text-xs bg-yellow-100 text-yellow-700">Sin stock</span>
    }
    return (
      <span className="px-2 py-0.5 rounded-full text-xs bg-green-100 text-green-700">
        {conStock.length} publicación{conStock.length !== 1 ? 'es' : ''} activa{conStock.length !== 1 ? 's' : ''}
      </span>
    )
  }

  const precioMinimo = (cartas: CartaVendedor[]) => {
    const conStock = cartas.filter(c => c.stock > 0)
    if (conStock.length === 0) return null
    return Math.min(...conStock.map(c => c.price))
  }

  if (loading) {
    return (
      <div className="min-h-screen bg-orange-50 flex items-center justify-center">
        <p className="text-gray-500">Cargando favoritos...</p>
      </div>
    )
  }

  return (
    <div className="min-h-screen bg-orange-50 px-4 py-8">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-8">
          <h1 className="text-3xl font-bold text-gray-800 flex items-center gap-3">
            <span className="text-red-500">♥</span>
            Mis Favoritos
            <span className="text-lg font-normal text-gray-400">({favoritos.length})</span>
          </h1>
          <p className="text-gray-500 mt-1 text-sm">
            Tus cartas guardadas. Compará precios entre vendedores.
          </p>
        </div>

        {favoritos.length === 0 ? (
          <div className="bg-white rounded-2xl shadow-sm border border-orange-100 p-12 text-center">
            <div className="text-6xl mb-4">♡</div>
            <h2 className="text-xl font-semibold text-gray-700 mb-2">No tenés favoritos todavía</h2>
            <p className="text-gray-400 mb-6">Explorá las cartas y guardá las que te interesan.</p>
            <button
              onClick={() => navigate('/cards')}
              className="bg-orange-500 hover:bg-orange-600 text-white px-6 py-2 rounded-full font-semibold transition"
            >
              Ver cartas
            </button>
          </div>
        ) : (
          <div className="space-y-4">
            {favoritos.map(entry => {
              const isExpanded = expandido === entry.id
              const minPrecio = entry.cartas ? precioMinimo(entry.cartas) : null
              const coverImage = entry.cartas?.find(c => c.image)?.image

              return (
                <div
                  key={entry.id}
                  className="bg-white rounded-2xl shadow-sm border border-orange-100 overflow-hidden"
                >
                  {/* Card row */}
                  <div className="flex items-center gap-4 p-4">
                    {/* Imagen */}
                    <div className="w-16 h-20 flex-shrink-0 rounded-lg overflow-hidden bg-gray-100 border border-gray-200">
                      {coverImage ? (
                        <img src={coverImage} alt={entry.cartaClass?.name} className="w-full h-full object-cover" />
                      ) : (
                        <div className="w-full h-full flex items-center justify-center text-gray-300 text-2xl">?</div>
                      )}
                    </div>

                    {/* Info */}
                    <div className="flex-1 min-w-0">
                      <div className="flex items-start justify-between gap-2">
                        <div>
                          <h3 className="font-bold text-gray-800 text-lg leading-tight">
                            {entry.cartaClass?.name ?? 'Carta eliminada'}
                          </h3>
                          {entry.cartaClass?.description && (
                            <p className="text-xs text-gray-400 mt-0.5 line-clamp-1">{entry.cartaClass.description}</p>
                          )}
                        </div>
                        {/* Botón quitar */}
                        {entry.cartaClass && (
                          <button
                            onClick={() => handleRemove(entry.cartaClass!.id)}
                            className="text-red-400 hover:text-red-600 text-xl flex-shrink-0 transition"
                            title="Quitar de favoritos"
                          >
                            ♥
                          </button>
                        )}
                      </div>

                      <div className="flex items-center gap-3 mt-2 flex-wrap">
                        {getBadge(entry)}
                        {minPrecio !== null && (
                          <span className="text-orange-600 font-semibold text-sm">
                            Desde ${minPrecio.toLocaleString('es-AR')}
                          </span>
                        )}
                      </div>
                    </div>

                    {/* Botón expandir */}
                    {entry.disponible && entry.cartas.length > 0 && (
                      <button
                        onClick={() => setExpandido(isExpanded ? null : entry.id)}
                        className="flex-shrink-0 px-4 py-2 rounded-xl bg-orange-50 hover:bg-orange-100 text-orange-600 font-semibold text-sm transition flex items-center gap-1"
                      >
                        {isExpanded ? 'Ocultar' : 'Comparar'}
                        <span className={`transition-transform ${isExpanded ? 'rotate-180' : ''}`}>▼</span>
                      </button>
                    )}
                  </div>

                  {/* Tabla comparación */}
                  {isExpanded && (
                    <div className="border-t border-orange-100 px-4 pb-4 pt-3">
                      <p className="text-xs text-gray-400 mb-3 font-medium uppercase tracking-wide">
                        Comparativa de vendedores
                      </p>
                      <div className="overflow-x-auto">
                        <table className="w-full text-sm">
                          <thead>
                            <tr className="text-left text-xs text-gray-400 border-b border-gray-100">
                              <th className="pb-2 font-medium">Vendedor</th>
                              <th className="pb-2 font-medium">Precio</th>
                              <th className="pb-2 font-medium">Stock</th>
                              <th className="pb-2 font-medium">Rareza</th>
                              <th className="pb-2 font-medium">Valoración</th>
                              <th className="pb-2 font-medium"></th>
                            </tr>
                          </thead>
                          <tbody>
                            {entry.cartas.map(carta => (
                              <tr key={carta.id} className="border-b border-gray-50 hover:bg-orange-50 transition">
                                <td className="py-2.5 font-medium text-gray-700">
                                  {carta.uploader?.nombre ?? 'Desconocido'}
                                </td>
                                <td className="py-2.5">
                                  <span className={`font-bold ${carta.stock > 0 ? 'text-orange-600' : 'text-gray-400'}`}>
                                    ${carta.price.toLocaleString('es-AR')}
                                  </span>
                                </td>
                                <td className="py-2.5">
                                  <span className={`px-2 py-0.5 rounded-full text-xs font-medium ${
                                    carta.stock > 0
                                      ? 'bg-green-100 text-green-700'
                                      : 'bg-red-100 text-red-500'
                                  }`}>
                                    {carta.stock > 0 ? `${carta.stock} disp.` : 'Agotado'}
                                  </span>
                                </td>
                                <td className="py-2.5 text-gray-500 text-xs">{carta.rarity ?? '—'}</td>
                                <td className="py-2.5">
                                  {carta.uploader && carta.uploader.reviewsCount > 0 ? (
                                    <span className="flex items-center gap-1 text-xs">
                                      <span className="text-yellow-400">
                                        {'★'.repeat(Math.round(carta.uploader.rating))}
                                        {'☆'.repeat(5 - Math.round(carta.uploader.rating))}
                                      </span>
                                      <span className="text-gray-400">({carta.uploader.reviewsCount})</span>
                                    </span>
                                  ) : (
                                    <span className="text-xs text-gray-300">Sin reseñas</span>
                                  )}
                                </td>
                                <td className="py-2.5">
                                  <button
                                    onClick={() => navigate(`/card/${carta.id}`)}
                                    disabled={carta.stock === 0}
                                    className="px-3 py-1 rounded-full text-xs font-semibold transition
                                      bg-orange-500 hover:bg-orange-600 text-white
                                      disabled:bg-gray-200 disabled:text-gray-400 disabled:cursor-not-allowed"
                                  >
                                    Ver
                                  </button>
                                </td>
                              </tr>
                            ))}
                          </tbody>
                        </table>
                      </div>
                    </div>
                  )}
                </div>
              )
            })}
          </div>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Verificar compilación TypeScript del frontend**

```bash
cd vite-project/vite-project-ts && npx tsc --noEmit 2>&1
```
Expected: sin errores.

---

### Task 5: Botón corazón en CardDetail.tsx

**Files:**
- Modify: `vite-project/vite-project-ts/src/pages/CardDetail.tsx`

- [ ] **Step 1: Agregar estado y lógica del corazón**

Agregar imports al inicio (después de los existentes):
```tsx
import { useEffect, useRef, useState, useCallback } from 'react'
```
(reemplazar la línea de imports de React existente con esta)

Agregar dentro del componente `CardDetail`, después de `const [loading, setLoading] = useState(true)`:
```tsx
const [enFavoritos, setEnFavoritos] = useState(false)
const [wishlistLoading, setWishlistLoading] = useState(false)
```

Agregar este `useEffect` después del `useEffect` de `fetchCard`:
```tsx
useEffect(() => {
  if (!user || user.role !== 'user' || !card?.cartaClass?.id) return
  fetchApi('/api/wishlist')
    .then(r => r.json())
    .then(json => {
      const entries = json.data || []
      const found = entries.some((e: any) => e.cartaClass?.id === card.cartaClass.id)
      setEnFavoritos(found)
    })
    .catch(() => {})
}, [card, user])
```

Agregar esta función antes del `return`:
```tsx
const handleToggleWishlist = async () => {
  if (!card?.cartaClass?.id || wishlistLoading) return
  setWishlistLoading(true)
  try {
    if (enFavoritos) {
      await fetchApi(`/api/wishlist/${card.cartaClass.id}`, { method: 'DELETE' })
      setEnFavoritos(false)
    } else {
      await fetchApi('/api/wishlist', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ cartaClassId: card.cartaClass.id }),
      })
      setEnFavoritos(true)
    }
  } catch {
    // silencioso
  } finally {
    setWishlistLoading(false)
  }
}
```

- [ ] **Step 2: Agregar el botón corazón al JSX**

Ubicar el bloque del precio (línea ~165 en el original):
```tsx
{/* Precio */}
<div className="flex items-center gap-3">
  <span className="text-3xl font-bold text-gray-800">${card.price}</span>
</div>
```

Reemplazarlo con:
```tsx
{/* Precio */}
<div className="flex items-center gap-3">
  <span className="text-3xl font-bold text-gray-800">${card.price}</span>
  {user?.role === 'user' && card?.cartaClass?.id && (
    <button
      onClick={handleToggleWishlist}
      disabled={wishlistLoading}
      title={enFavoritos ? 'Quitar de favoritos' : 'Agregar a favoritos'}
      className={`text-3xl transition disabled:opacity-40 ${
        enFavoritos ? 'text-red-500 hover:text-red-400' : 'text-gray-300 hover:text-red-400'
      }`}
    >
      {enFavoritos ? '♥' : '♡'}
    </button>
  )}
</div>
```

- [ ] **Step 3: Verificar compilación**

```bash
cd vite-project/vite-project-ts && npx tsc --noEmit 2>&1
```
Expected: sin errores.

---

### Task 6: Opción en Header + ruta en App.tsx

**Files:**
- Modify: `vite-project/vite-project-ts/src/components/Header.tsx`
- Modify: `vite-project/vite-project-ts/src/App.tsx`

- [ ] **Step 1: Agregar opción "Mis Favoritos" en el dropdown del Header**

Ubicar el bloque del perfil en el dropdown (el `<button onClick={handleProfileClick}>`):
```tsx
<button onClick={handleProfileClick} className="block w-full text-left px-4 py-2 hover:bg-orange-100">
  Mi Perfil
</button>
```

Reemplazarlo con:
```tsx
<button onClick={handleProfileClick} className="block w-full text-left px-4 py-2 hover:bg-orange-100">
  Mi Perfil
</button>

{user.role === 'user' && (
  <button
    onClick={() => { setUserMenuOpen(false); navigate('/wishlist') }}
    className="block w-full text-left px-4 py-2 hover:bg-orange-100 text-red-500 font-medium"
  >
    ♥ Mis Favoritos
  </button>
)}
```

- [ ] **Step 2: Agregar import y ruta en App.tsx**

Agregar el import después de los imports existentes de páginas:
```tsx
import WishlistPage from "./pages/WishlistPage";
```

Agregar la ruta después de la ruta `purchases`:
```tsx
{/* Wishlist */}
<Route
  path="wishlist"
  element={
    <ProtectedRoute>
      <WishlistPage />
    </ProtectedRoute>
  }
/>
```

- [ ] **Step 3: Verificar compilación final**

```bash
cd vite-project/vite-project-ts && npx tsc --noEmit 2>&1
```
Expected: sin errores.
