// imageUtils.ts
// Responsabilidad: helpers de procesamiento de imagen compartidos entre servicios.
// Funciones típicas: redimensionar, ajustar contraste/brillo, convertir formatos,
// normalizar buffers de entrada (base64 → Buffer, URL → Buffer, etc.).

// TODO: implementar helpers de imagen
// import sharp from 'sharp';  ← descomentar al implementar
// Ejemplo de helpers a agregar:
//   - resizeToMaxDimension(buffer, maxPx): Buffer
//   - enhanceContrast(buffer, factor): Buffer
//   - toGrayscale(buffer): Buffer
//   - base64ToBuffer(b64: string): Buffer

export {};
